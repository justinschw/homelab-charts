# csi-storage

> Helm chart to manage CSI-related storage resources (StorageClass) for homelab.

## Install

```
helm install my-csi ./csi-storage
```

## Values

See `values.yaml` for configurable parameters (storage class name, provisioner, etc.).